#include "Form.hpp"
#include "kiss_sdl/kiss_sdl.h"
#include "../util/debugging/notif.h"
UIForm::UIForm(int x, int y) : GameObject(NULL, x, y, EUI) {
	// stuff
}
void UIForm::Add(Widget& w, int x, int y) {
	OoO();
}
void UIForm::Remove(std::string& id) {
	OoO();
}